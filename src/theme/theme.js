import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#1b5835',
    },
    secondary: {
      main: '#f50057',
    },
  },
});

export default theme;