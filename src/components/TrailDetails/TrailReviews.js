import React from 'react';
import { Typography, Box } from '@mui/material';

const TrailReviews = ({ trail }) => {
  return (
    <Box>
      <Typography variant="h6" gutterBottom>
        Reviews
      </Typography>
      {/* Render trail reviews */}
    </Box>
  );
};

export default TrailReviews;